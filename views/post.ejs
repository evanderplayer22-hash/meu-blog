<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= post.title %> - Meu Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">Meu Blog</a>
      <a href="/" class="btn btn-outline-light ms-auto">← Todos posts</a>
    </div>
  </nav>

  <div class="container my-5">
    <article class="blog-post">
      <h1 class="display-4 fw-bold mb-4"><%= post.title %></h1>
      <div class="meta mb-4">
        <i class="fas fa-calendar text-primary"></i> <%= post.date %> 
        <i class="fas fa-user ms-3 text-primary"></i> <%= post.author %>
      </div>
      <% if(post.image) { %>
        <img src="<%= post.image %>" class="img-fluid rounded mb-4 shadow-sm" alt="<%= post.title %>">
      <% } %>
      <div class="post-content lead"><%= post.content %></div>
    </article>
  </div>

  <footer class="bg-dark text-white py-4 mt-auto">
    <div class="container text-center">
      <p>© 2026 Meu Blog</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
